# Production build: server serves the built client from the same origin
# (see server/src/app.ts), so the API is called as a relative path instead
# of an absolute localhost URL. Safe to commit — not a secret, just config.
VITE_API_URL=/api
